Converted outputs for downstream training + retrieval.

- `all_scored.csv`: all discovered datasets with relevance scores.
- `relevant_datasets.csv`: score-filtered datasets used for fetch.
- `layer_summaries.jsonl`: one layer-level narrative + stats record.
- `text_records.jsonl`: one feature-level narrative record.
- `text_corpus/*.md`: markdown per layer for local corpus inspection.
